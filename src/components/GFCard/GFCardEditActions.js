import styled from 'styled-components';

const GFCardEditActions = styled.div`
  text-align: right;
  width: 100%;
  position: absolute;
  padding: .5em;
  z-index: 1;
`;

export default GFCardEditActions;
